import { BulletBoxDirective } from './bullet-box.directive';

describe('BulletBoxDirective', () => {
  it('should create an instance', () => {
    const directive = new BulletBoxDirective();
    expect(directive).toBeTruthy();
  });
});
